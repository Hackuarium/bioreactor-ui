import { createContext } from 'react';

export const StatusParameterContext = createContext(0);
export const ErrorParameterContext = createContext(0);
export const StepParameterContext = createContext(0);
export const StepsProtocolParameterContext = createContext(0);